---
title: "FILL IN — manuscript title"
author: "FILL IN — Your Name"
date: today
---

<!--
This is the clean manuscript. It contains text only — no analysis code.
Figures and tables are pulled in from outputs/, which analysis.qmd
produces. Always render analysis.qmd first if you've changed any code,
so these references are up to date.

Notice how short this frontmatter is: format, bibliography, and csl
aren't repeated here because _quarto.yml already sets them for the
whole project. Only add settings here if this file needs to override
the project default (like analysis.qmd does, to render html-only).
-->

## Abstract

FILL IN — 150–250 words summarising the question, approach, and key finding.

## Introduction

FILL IN. Cite sources with the `[@key]` syntax once you've added entries
to `references/references.bib`, e.g. [@example2024].

## Methods

### Study design

FILL IN.

### Statistical analysis

FILL IN — describe the model(s) from `analysis.qmd` in prose here.

## Results

```{r}
#| label: setup-results
#| echo: false

library(tidyverse)
summary_table <- read_csv("outputs/tables/summary_table.csv")
report_stats <- readRDS("outputs/report_stats.rds")
```

<!--
FILL IN. This setup chunk re-loads what analysis.qmd already computed and
saved — it doesn't repeat any analysis. The rule of thumb: any filtering,
subsetting, or computation happens in analysis.qmd and gets saved to
outputs/; this file only ever reads. Use inline code (single backtick, r,
a space, your expression, single backtick) in a sentence below to keep a
reported number in sync with your actual results — e.g.
round(report_stats$mean_treatment, 1) or round(report_stats$pvalue, 3)
— instead of a hardcoded value someone forgets to update after a revision.
-->

FILL IN — describe the result in prose, referencing the figure and table produced by `analysis.qmd`:

![FILL IN — figure caption matching the one in analysis.qmd](outputs/figures/fig-example.png){#fig-example}

See @fig-example and @tbl-summary-ref.

```{r}
#| label: tbl-summary-ref
#| tbl-cap: "FILL IN — table caption"
#| echo: false

knitr::kable(summary_table)
```

## Discussion

FILL IN.

## Data availability

<!--
FILL IN once you've set up Zenodo (see the full guide, chapter 4):

Data and code supporting this study are available on Zenodo at
https://doi.org/10.5281/zenodo.XXXXXXX (will be made publicly available
upon acceptance) and on GitHub at
https://github.com/yourusername/your-repo-name.
-->

## References

::: {#refs}
:::
